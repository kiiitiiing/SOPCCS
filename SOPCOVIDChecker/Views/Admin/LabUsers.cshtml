
@{
    ViewData["Title"] = "LAB Users";
}

<div class="col-md-12">
    <div class="card card-success card-outline">
        <div class="card-header">
            <form class="fa-pull-right form-inline ml-3" asp-controller="Admin" asp-action="LabUsersPartial" method="get" onsubmit="return partialViewGet(this, '#admin_labusers');">
                <div class="input-group input-group-sm">
                    <input class="form-control form-control-navbar" id="q" name="q" placeholder="Search">
                    <div class="input-group-append">
                        <button class="btn btn-success" type="submit" title="Search User">
                            <i class="fas fa-search"></i>
                        </button>
                    </div>
                    <div class="input-group-append">
                        <a class="btn btn-primary" title="Add User" onclick="showInPopup('@Url.Action("Register", "Account", new { level = "LAB" })', 'def')">
                            <i class="fas fa-plus"></i>
                        </a>
                    </div>
                </div>
            </form>
            <h4>LAB Users</h4>
        </div>
        <div class="card-body" id="admin_labusers">
        </div>
    </div>
</div>

<script>
    LoadPartialView('#admin_labusers', '@Url.Action("LabUsersPartial","Admin")');
</script>